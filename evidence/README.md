# evidence

Deterministic observations bound to exact Repository Pins. Generated by `procedures/boundary-drift-recon-v0.md`.

**Schema:** `schemas/evidence.schema.json`

**Navigation:** Each evidence artifact links to a Repository Pin and is referenced by Findings and Drift Records. Navigate: `findings/*.json` → `evidence/*.json` → `pins/*.json`.

**MVP constraint (FR-CON-008):** No source excerpts — only pins, paths, hashes, counts, manifests, and derived records.
