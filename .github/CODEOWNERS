# CODEOWNERS — the highest-risk classes require the owner's review before merge.
# Per CLAUDE.md §6a. This file only takes effect once the owner enables branch
# protection on the default branch with "Require review from Code Owners".
# Then the operator cannot merge changes to these paths without the owner's click
# — authority, not technical review.

# The operator constitution itself
/CLAUDE.md            @kimeisele

# CI, workflows, and anything under .github (permissions, automation)
/.github/             @kimeisele
