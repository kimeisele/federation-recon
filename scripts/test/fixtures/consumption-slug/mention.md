# Slug-only fixture

This document mentions federation-recon without citing any Finding ID.

Deliberately contains no `finding-` identifier, so the repo-slug pattern is the
only thing that can match here. That is what makes this a genuine positive
control for the slug channel: break the slug pattern and this test must fail.
