# Repository Manifest (draft)

Explicit list of repositories `federation-recon` observes. Required input to any Procedure Manifest (founding package §12.2). This is preparation only — no procedure may run against this list until the founding decision record is adopted.

Recon includes itself per FR-CON-011.

| Repository | Role (observed, not authoritative) | Ref to observe |
|---|---|---|
| `federation-recon` | self-observation (FR-CON-011) | `main` |
| `steward-protocol` | protocol / verification primitives | TBD |
| `agent-world` | world / boundary claims source | TBD |
| `agent-internet` | optional repository graph / crawl source (not consumed in Slice v0) | TBD |
| `steward` | Finding consumer, future remediation planning | TBD |
| `agent-city` | local runtime, execution claims source | TBD |
| `steward-federation` | transport repository | TBD |

Fill in exact owner/repo slugs and resolvable refs before drafting the first Procedure Manifest.
